{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "submodules/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --scope react-micro-app --scope vue-micro-app"
  },
  "devDependencies": {
    "lerna": "^6.3.0"
  },
  "engines": {
    "node": ">=16"
  },
  "engineStrict": true,
  "repository": "https://github.com/ziyi2/micro-framework.git"
}